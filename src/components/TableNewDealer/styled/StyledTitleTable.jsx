import styled from 'styled-components'

const StyledTitleTable = styled.div`
  display: inline-block;
  text-transform: uppercase;
  font-family: var(--fontGilroySemiBold);
  font-size: 14px;
  line-height: 20px;
`
export default StyledTitleTable
