import styled from 'styled-components';

const StyledTitleServer = styled.div`
        font-family: var(--fontGilroyRegular);
        font-size: 14px;
        line-height: 20px;
        letter-spacing: 0.16px;
        color: var(--mainblack);
        margin-bottom: 2px;
`
export default StyledTitleServer;