import styled from 'styled-components';

const StyledSelectedOptions = styled.div`
        font-family: var(--fontGilroySemiBold);
        font-size: 14px;
        line-height: 20px;
        letter-spacing: 0.16px;
        color: var(--mainblack);
        margin-bottom: 2px;
        padding-bottom: 17px;
`
export default StyledSelectedOptions;