import styled from 'styled-components'
import { DEVICE } from '../../../../../constants/media'

const StyledSiderTop = styled.div`
  height: 56px; 
  position: relative;
  
	@media ${DEVICE.tabletDevices1250} {
	 
  }
`

export default StyledSiderTop
